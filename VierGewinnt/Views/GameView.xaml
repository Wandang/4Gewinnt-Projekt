<this:IView x:Class="VierGewinnt.Views.GameView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:this="clr-namespace:VierGewinnt"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="735"
	Title="GameView" PreviewKeyDown="GameView_OnPreviewKeyDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="19*"/>
            <RowDefinition Height="150*"/>
            <RowDefinition Height="31*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="28*"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="28*"/>
        </Grid.ColumnDefinitions>

        <Canvas Name="Canv" Grid.Column="1" Grid.Row="1" Loaded="FrameworkElement_OnLoaded" />
    </Grid>
</this:IView>
